<UserControl x:Class="Ksiegarnia.Views.WypozyczeniaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lista Wypożyczeń" FontSize="22" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Rentals}"
          AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"          Binding="{Binding IDwyp}"       Width="50"/>
                <DataGridTextColumn Header="Książka"     Binding="{Binding Ksiazka}"     Width="*"/>
                <DataGridTextColumn Header="Czytelnik"   Binding="{Binding Czytelnik}"   Width="*"/>
                <DataGridTextColumn Header="Od"          Binding="{Binding DataOD}"      Width="*"/>
                <DataGridTextColumn Header="Do"          Binding="{Binding DataDO}"      Width="*"/>
                <DataGridTextColumn Header="Oddano"      Binding="{Binding DataOddania}" Width="*"/>
                <DataGridTemplateColumn Header="Akcje" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                <Button Content="Edytuj"
                Command="{Binding DataContext.EditClientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                CommandParameter="{Binding}"
                Margin="2"/>

                                <Button Content="Usuń"
                Command="{Binding DataContext.DeleteClientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                CommandParameter="{Binding}"
                Margin="2"/>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Border Background="#34495E" Grid.Row="2" Height="50" Width="210">
            <Button Content="DODAJ"
Command=""
CommandParameter="ksiazki"
Margin="5" Height="40" Width="200" HorizontalAlignment="Center" Background="LightGreen"/>
        </Border>
    </Grid>
</UserControl>