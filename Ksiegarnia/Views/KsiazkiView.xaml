<UserControl x:Class="Ksiegarnia.Views.KsiazkiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lista Książek" FontSize="22" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Books}"
          AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"    Binding="{Binding ID_ksiazki}" Width="50"/>
                <DataGridTextColumn Header="Tytuł" Binding="{Binding Nazwa}"      Width="*"/>
                <DataGridTextColumn Header="Autor" Binding="{Binding Autor}"      Width="*"/>
                <DataGridTextColumn Header="Cena"  Binding="{Binding Cena}"       Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>